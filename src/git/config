################
#  GIT CONFIG  #
################

[user]
    username = S1SYPHOS
    name = Martin Folkers
    email = hello@twobrain.io

[credential]
    helper = cache --timeout=3600

[core]
	editor = vim
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
    excludesfile = ~/.local/etc/git/ignore

[color]
    # Available options: normal, red, green, yellow, blue, magenta, cyan, white & black
    ui = auto
    interactive = auto
    status = auto
	diff = auto
	branch = auto

[commit]
	verbose = true

[push]
	default = simple

[pull]
	ff = only

[fetch]
    prune = true

[rebase]
    autosquash = true

[merge]
	tool = vimdiff

[alias]
